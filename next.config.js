/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export'
  images: {
    domains: [
      "structuralengineeringbasics.com",
      "media.sortly.com",
      "digital-bucket.prod.bfi.co.id",
      "media.istockphoto.com",
    ],
  },
};

module.exports = nextConfig;
